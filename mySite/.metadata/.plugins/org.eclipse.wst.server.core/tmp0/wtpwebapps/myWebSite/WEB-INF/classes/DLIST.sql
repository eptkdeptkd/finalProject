DROP TABLE DLIST
CASCADE CONSTRAINTS

CREATE TABLE DLIST(
	SEQ NUMBER(8) PRIMARY KEY,
	MENUSEQ NUMBER(8) NOT NULL,
	
	REF NUMBER(8) NOT NULL,
	STEP NUMBER(8) NOT NULL,
	DEPTH NUMBER(8) NOT NULL,
	
	ID VARCHAR2(50) NOT NULL,
	TITLE VARCHAR2(300) NOT NULL,
	CONTENTS VARCHAR2(4000) NOT NULL,
	WDATE DATE NOT NULL,
	
	DEL NUMBER(1) NOT NULL,
	READCOUNT NUMBER(8) NOT NULL
)

CREATE SEQUENCE SEQ_DLIST
START WITH 1
INCREMENT BY 1

ALTER TABLE DLIST
ADD CONSTRAINT FK_DLIST_MENUSEQ FOREIGN KEY(MENUSEQ)
REFERENCES DMENU(SEQ)

ALTER TABLE DLIST
ADD CONSTRAINT FK_DLIST_ID FOREIGN KEY(ID)
REFERENCES DMEMBER(ID)

ALTER TABLE DLIST
ADD SECRETLIST NUMBER(1)















